import React from 'react';
import { shallow } from 'enzyme';
import { {{pascalCase name}} } from '../{{name}}';
import { Component{{pascalCase name}}Props } from '../{{name}}.d';

describe('{{pascalCase name}}', () => {
  let defaultProps: JSX.IntrinsicAttributes & Component{{pascalCase name}}Props;

  beforeEach(() => {
    defaultProps = {
      children: "{{pascalCase name}}"
    }
  });

  it('renders correctly', () => {
    const wrapper = shallow(
      <{{pascalCase name}} {...defaultProps}/>
    );
    expect(wrapper.debug()).toMatchSnapshot();
  });

  it('mount correctly', () => {
    const wrapper = shallow(
      <{{pascalCase name}} {...defaultProps} />
    );
    expect(() => wrapper).not.toThrow();
  });

  it('Should contain defaultProps:children correctly', () => {
    const wrapper = shallow(
      <{{pascalCase name}} {...defaultProps} />
    );
    expect(() => wrapper).not.toThrow();
    expect(wrapper.html()).toContain('{{pascalCase name}}');
  });

});
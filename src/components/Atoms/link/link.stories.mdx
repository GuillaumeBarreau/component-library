import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { Link } from './link';
import { ComponentLinkProps } from "./link.d";

<Meta 
  title="Components/Atoms/Link" 
  component={ Link }
  argTypes={ {
    className: {
      description: "Override a component's styles",
      control: "text",
    },
    children: {
      control: "text",
      description: "Children of the link.",
    }
  } }
  args={ {
    children: "children",
    className: "Override className"
  } }/>

## React Component

#### Import library

```
Import {Atoms} from '@bonjour-les-pixels/component-library';
```

#### Use Link component

```
<Atoms.Link {...props}> CHILDREN_TEXT </Atoms.Link>
```

## Playground

<Canvas>
  <Story name="Link">
    {args => {
      return <Link {...args}  />;
    }}
  </Story>
</Canvas>